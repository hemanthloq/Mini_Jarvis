# Core server
fastapi>=0.115
uvicorn[standard]>=0.30
python-dotenv>=1.0

# Voice pipeline
openwakeword>=0.6      # wake word ("hey jarvis" pretrained model), ONNX on Windows
onnxruntime>=1.18
sounddevice>=0.5       # mic capture + audio playback (bundled PortAudio)
websockets>=12.0       # Deepgram streaming STT (raw ws, no SDK drift)
httpx>=0.27            # ElevenLabs streaming TTS

# Smart path (Groq) uses httpx above — no extra SDK needed.
# Optional: only if you switch the smart path back to Anthropic Claude
# (see the note at the bottom of backend/router/smart_path.py).
# anthropic>=0.40

# Fast-path router
rapidfuzz>=3.9

# Audio post-processing (Jarvis voice effects)
numpy>=1.26
scipy>=1.13

# Windows system control
pycaw>=20240210        # volume + per-app audio ducking
comtypes>=1.4
keyboard>=0.13         # media keys + push-to-talk hotkey
screen-brightness-control>=0.24
psutil>=6.0            # REAL system health (CPU/RAM/disk/battery)

# Wake-word training (scripts/train_wake_jarvis.py - training only, not runtime)
scikit-learn>=1.5
pyttsx3>=2.90          # local SAPI voices to synthesize training samples

# Reading Windows notifications aloud (best-effort; needs notification access
# granted to Python in Windows Settings). Optional — the feature degrades cleanly.
winsdk>=1.0

# Media
spotipy>=2.24
send2trash
# keyless web search that RETURNS TEXT, so the assistant can answer from real
# current info instead of stale training data (router/tools.py look_up)
ddgs
